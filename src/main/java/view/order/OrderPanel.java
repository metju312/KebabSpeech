package view.order;

import javax.swing.*;
import java.awt.*;

public class OrderPanel extends JPanel{

    public OrderPanel() {
        add(new JButton("OrderPanel"));
        setMinimumSize(new Dimension(600,300));
    }
}
